using _Team;
using Characters;
using Stats;
using UnityEngine;

namespace _CombatSystem
{
    // This class only exits to specify the object type to one more clear in the context of Combat (instead Character
    // which is ambiguous)
    public class CombatEvents : CombatCharacterEventsBase
    {
        
    }
}
